from langchain_core.tools import tool


# @tool(parse_docstring=True)
# def get_firecrawl_data(url: str) -> str:
#     """
#     Get the data from the url using firecrawl.
#     """
#     return firecrawl.get_data(url)


tools = []
